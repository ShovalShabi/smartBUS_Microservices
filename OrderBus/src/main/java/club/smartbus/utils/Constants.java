package club.smartbus.utils;

public class Constants {
    public static final String DEFAULT_PAGE_SIZE = "30";
    public static final String DEFAULT_PAGE = "0";
    public static final Boolean DEFAULT_ORDER_BUS = false;
    public static final String ROUTES_SERVICE_URL = "http://localhost:10094/route";
    public static final int MAX_RETRIES = 3; // Max retries for sending message via WebSocket protocol
    public static final String ORDER_BUS_CLIENT = "OrderBus"; // Max retries for sending message via WebSocket protocol
    public static final String DRIVER_CONSOLE_CLIENT = "DriverConsole"; // Max retries for sending message via WebSocket protocol

}
